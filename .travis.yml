language: node_js

node_js: 12.10.0

cache:
  directories:
    - node_modules

install:
  - yarn install

script:
  - yarn build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $EZDIAGRAM 
  on:
    branch: gh-pages